# ==============================================================
#  造化玉碟 · 独立版 — .gitignore
#  规则依据：对当前磁盘实际内容的逐项实测（2026-09-22）
# ==============================================================

# --------------------------------------------------------------
# ① 密钥与个人游戏数据 —— 绝不进仓库
# --------------------------------------------------------------
# server/data/settings.json 里存着真实可用的 API Key。
# 已确认 server/index.js 启动时会自建 data 下三个子目录、
# 读不到配置就用内置默认值，所以整目录排除后别人 clone 照样能跑。
server/data/
server/dev-server.log

# 防呆：历史设置备份（archive/）里每一份都原样复制了同一把 API Key。
# 该目录已不存在，但规则保留 —— 一旦再被建出来，也绝不进仓库。
archive/

# --------------------------------------------------------------
# ⚠ 隐私：client/src/prompts/ —— 本规则**故意不排除它**，请勿加回来
# --------------------------------------------------------------
# 这个目录放的是「发给 AI 的所有字」（协议 / 预设 / 契约 / 演化 / 助手 / 模板 / 令牌），
# 作者手写的全文不随仓库公开。做法是「同名文件、两份内容」：
#   · 仓库里跟踪的那一份 = **空壳占位版**（导出名齐全、内容为空，见该目录 README.md）；
#   · 本机工作区里那一份 = **真版**，靠 git 的 skip-worktree 标记让 git 忽略这个差异
#     —— 实测：`git status` 显示干净，`git add -A` 一个字都不暂存，
#     连 `git checkout -- 文件` / `git restore 文件` 都会被 git 拒绝执行，真版不会被覆盖。
# ⇒ 别人 clone 拿到空壳：能构建、能启动（没有内置文案）；本机跑起来与改造前逐字一致。
# ⚠ 真版的另一份备份在 client/_unused/prompt-source/（已随下面 ③ 排除）。
# ⚠ 要更新仓库里那份空壳，跑 client/_unused/tmp-rest/发布提示词占位版.py。

# 防呆：提示词清单是生成物，内容＝玩家当前设置的全套（预设全文 + 世界书）。
# 它的正式位置是 client/_unused/prompt-inventory/（被下面 ③ 的规则排除），
# 这里再钉一条：万一哪天生成命令又被指回源码目录，也拦住。
client/src/prompts/提示词清单.md

# --------------------------------------------------------------
# ② 依赖与构建产物
# --------------------------------------------------------------
node_modules/

# vite 构建产物，clone 后 npm run build 重建
client/dist/

# vite 每次启动生成的临时配置快照
client/vite.config.js.timestamp-*.mjs

# --------------------------------------------------------------
# ③ 隔离区 —— 集中收纳「删了也照样能玩」的东西（2026-09-22 设立）
# --------------------------------------------------------------
# 里面是：文档 docs/、工具 tools/、打包脚本 dev/、桌面壳 desktop/、
# 预设留档 snapshot-v2-preset/ 与两份根目录预设 JSON、README、
# 原始模型素材 VRM/ ACT/ _models/、开发期探针工作区、以及旧的 dist。
# 清单与恢复办法见 client/_unused/README-隔离说明.md。
#
# ⚠ 该目录放在 client/ 里面不是随手放的：battletest/ 与 scene3d 两套
# 检查件靠「自己位置往上两级＝client」定位项目（约 75 个文件都这么写），
# 只有与原来深度一致才能一个文件都不改继续跑。详见说明文件。
client/_unused/

# --------------------------------------------------------------
# ④ 素材源（游戏运行时读 client/public/models，不读这些）
# --------------------------------------------------------------
# 注意：以下两条必须带前导斜杠，只匹配本目录。
# 写成 VRM/ 会连 client/public/models/vrm/ 一起挡掉
# （Windows 下 git 默认大小写不敏感），那 18 个模型是游戏必需的。
#
# 这两份源现已收进 client/_unused/；规则保留作防呆，
# 免得哪天真把源又解回项目根时被误提交。
/VRM/
/ACT/

# --------------------------------------------------------------
# ⑤ 通用
# --------------------------------------------------------------
*.bak
*.bak-*
*.bak.js
*.bak.jsx
*.log
.DS_Store
Thumbs.db

# --------------------------------------------------------------
# ⑥（可选）仓库瘦身开关 —— 当前默认全部保留（约 460 MB）
# --------------------------------------------------------------
# nature / characters / tex 已在 client/public/models/README.md
# 声明为 CC0 公共领域素材，可自由分发。
# vrm 目录的来源与授权未在 README 中声明（文件名是 pixiv 风格编号），
# 公开前请自行确认每个模型的再分发许可。
#
# 想瘦身就把下面几行取消注释：仓库可从约 460 MB 降到约 10 MB，
# 代价是别人 clone 后 3D 战场没有模型（文字玩法不受影响）。
# client/public/models/vrm/
# client/public/models/nature/
# client/public/models/characters/
# client/public/tex/
