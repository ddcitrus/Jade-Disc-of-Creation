// ===== 提示词 · 输出契约 / 视点 / 字数（公开占位版）=====
//
// 本文件在公开仓库里是空壳：导出名一个不少（引用方靠这些名字工作），内容为空。
// 作者手写的提示词全文不随仓库公开；需要完整能力时向作者索取完整版覆盖回来即可，
// 不用改任何代码。空壳的作用是让整套源码依然能构建、能启动。
export function battleBeforeText(...args) { return ''; }
export function battleWordQuotaText(...args) { return ''; }
export const DEFAULT_OUTPUT_CONTRACT = '';
export const PLAYER_POV_CONTRACT = '';
export function newCharIntakeLines(...args) { return []; }
export function storyWordQuotaText(...args) { return ''; }
export const VIEWPOINT_CONTRACT = '';
